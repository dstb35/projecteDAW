{%  extends "layout.html.twig" %}
{% block content %}

	<div class="container-fluid">
		<h2><a href="{{ path('employee_add') }}">Añadir empleado</a><hr/></h2>
		<div class="row">
			{%  for employee in employees %}
				<div class="col-lg-3 text-center">
					<h4>
						<span class="label label-info">{{  employee.name }}</span>
						<a class="btn btn-danger" href="{{ path('employee_remove', {'id': employee.employeeid}) }}">Eliminar</a>
					</h4>
					<hr/>
				</div>
			{%  endfor %}
		</div>
	</div>
	<div class=""clearfix"></div>
{%  endblock %}